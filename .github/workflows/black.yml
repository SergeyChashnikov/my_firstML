name: Lint

on:
  push:
    branches: [ "testing1" ]
  pull_request:
    branches: [ "testing1" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
